# Day 034 - Journal

## Today I Learned
Today I learned about generators and how to use `yield` and `next()`.
I practiced:

```python
yield
next()
for
```
I also learned about generator expressions.

## Problems I Faced
I initially returned the whole list from my AI data stream instead of yielding each message individually.

## How I Solved It
I used a `for` loop with `yield` to produce each message one at a time.

## AI Engineering Connection
Generators are useful for streaming data and processing large AI datasets efficiently.

## Key Takeaway
Generators produce values one at a time instead of storing everything in memory.

## Tomorrow's Goal
Continue with the next Python engineering concept.
