# What is a loop?
A loops allows a program to repeat a block of code without writing it over an dover again.

# What is the difference between a for loop and a while loop?
A for loop is used when you know now many times you want to repeat the program meanwhile, a while loop is used when you aren"t sure the number of times the program is to be repeated.

# What does range(5) produce?
It  is used to count how many times the loop should repeat itself, range(5) produces the sequence 0, 1, 2, 3, 4.

# Why is range(1, 6) different from range(5)?
The range(1, 6) has two values the first value is show that it should count from 1 instead of 0 and the 6 shows how many times it should count It excludes 6 because the stop value is always excluded, regardless of where the range starts, while the range(5) starts counting from 0, it counts 5 values and ends at 4.

# What is an infinite loop?
An infinite loop is a loop whose conditions are always true and it continues counting forever.

# What does count += 1 mean?
count += 1 means the variable calls itself as a value in itself and adds 1 it can be written as count = count + 1

# When would you choose a for loop instead of a while loop?
I will choose a for loop when i know how many times i want it count.